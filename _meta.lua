local _ = require("l10n.gettext")

return {
    name        = "readertoolbar",
    fullname    = _("Reader toolbar"),
    description = _("Adds a shortcuts bar and book info panel to the reader menu."),
}
